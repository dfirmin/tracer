Keep the runtime small. Agents interpret ETL; code only snapshots text, schedules headless Claude sessions, validates structured outputs and writes artifacts. Do not add SQL/AST parsers, regex lineage extraction, notebook execution or database access. Preserve the exact ten-column CSV contract. Add behavioral tests for changes to recovery, evidence, graph validity or completeness. Run `python3 -m unittest discover -s tests -v` from this directory.
